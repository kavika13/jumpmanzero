include Constants
include Level11Resources

dim iInit

sub Main()

  if iInit=0
    iInit=1
    spawn(#scriptTSaur)
    spawn(#scriptTRSaur)
    spawn(#scriptPSaur)
  end if

end sub

sub reset()
  setext(#px,70)
  setext(#py,52)
  setext(#pz,7)
  setext(#pstat,#jsNORMAL)
end sub

